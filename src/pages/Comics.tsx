import SearchBar from '../components/SearchBar';

const Comics = () => {
	const handleSearch = () => {};
	return (
		<section>
			<SearchBar handleSearch={handleSearch} />
		</section>
	);
};

export default Comics;
