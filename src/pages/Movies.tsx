import SearchBar from '../components/SearchBar';

const Movies = () => {
	const handleSearch = () => {};
	return (
		<section>
			<SearchBar handleSearch={handleSearch} />
		</section>
	);
};

export default Movies;
